#include "main.h"

/**
 * _abs - To compute the absolute value of an integer
 * @n: The integer we're making use of
 *
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
